      <% @subscriptions.each do |sub| %>
          <h4><%= sub.org_name_eng %></h4>
          <br>
          <%= sub.email_address %>
          <%= link_to 'Show', news %>
          <%= link_to 'Edit', edit_news_path(news) %>
          <%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' } %>
          <hr>
          <br>
          <br>
      <% end %>